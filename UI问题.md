在桌面壳中：

存在的问题：
- 页面内容区域的宽度不能跟着窗口的大小动态调整，如截图1所示
- 没有限制窗口的最小宽度和高度
- 主题色没有在UI中体现，所有的按钮点击、悬停、被选中的效果全部目前全部都是白色或灰色
- 集合树的容器高度不能超过页面内容区域的高度，目前是没有做限制，会溢出
- 配置状态footer需要固定在底部
- 标题列的宽度太窄了

优化：
- 左侧集合树中，支持展开和收起，默认展开所有项，
- 删除底部footer的提示，在footer中显示当前所选集合的数量，已选中的、已处理的数量
- 删除选着控制按钮，在表格标题前加一个复选框，用于选择所有项
- 表格的列可以通过拖动定义宽度
- 去掉桌面窗口的滚动条，只保留表格区域的滚动条
- 隐藏window自带的标题栏，采用自定义标题栏
- 图标采用现成的图标库
- 按照参考线框图优化设计（图2），增加文献矩阵视图（高度还原飞书多维表格）