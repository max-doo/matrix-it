- 重启应用后，自动启动同步校验
- 增加重新同步功能（覆盖飞书中原来的条目），选中条目后，同步按钮高亮，如果选择了已同步的条目，会有弹窗提醒是否要重新同步已同步的条目，如果仅选中未同步的，则不需要确认弹窗。

- 分析字段的自定义设置中，删除字段类型选型，默认都是string
- 可以设置勾选元数据的同步字段

- 去掉多模态分析的功能及相关代码（待定）

- 字段创建的顺序（首次同步，飞书中）要按照默认的字段顺序，元数据按照默认的字段顺序上传，分析字段顺序按照设置页中的顺序